##########################
#						#
#	CRISIS OBJECTIVES	#
# (Previously 'MEANS') #
#						#
##########################

# crisobj_name = {
#	reward = 1			# The menace reward given when this objective is completed
# }
#
# OR
#
# crisobj_name = {
#	potential = {}		# Potential trigger (This = country). Defines availability of this objective to the country. The trigger is evaluated daily.
#	reward = {			# The base menace reward given when this objective is completed
#		base = 100
#
#		modifier = {	# Modifier(s) that adjust the reward amount
#			factor = 2
#			owner = { is_xenophobe = yes }
#		}
#	}
# }

### Hyperdimensional Exodus ###
crisobj_have_lasting_peace = {
	reward = {
		base = 150
		complex_trigger_modifier = { #subtract a bunch for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 18
		}
		round_to = 10 	#we want a solid number
		min = 50 		#and we don't want less than 50
	}
	recurring = yes
}

crisobj_hyperdim_exodus_research_tech = { # Same as cosmogenesis
	reward = {
		base = 100
		complex_trigger_modifier = { #subtract 5 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 5
		}
		round_to = 5	#we want a solid number
		min = 5		#and we don't want less than 50
	}
}

crisobj_explore_astral_rifts = {
	#potential = {
	#	has_astral_planes_dlc = yes
	#}
	reward = {
		base = 5000
		complex_trigger_modifier = { #subtract 100 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 100
		}
		round_to = 500	#we want a solid number
		min = 500		#and we don't want less than 50
	}
}

crisobj_disintegrate_systems = {
	reward = {
		base = 1000
		complex_trigger_modifier = { #subtract 20 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 20
		}
		round_to = 100	#we want a solid number
		min = 100		#and we don't want less than 50
	}
}

#crisobj_have_non_aggression_pacts = {
#	reward = {
#		base = 250
#		complex_trigger_modifier = { #subtract 10 for each playable country that exists
#			trigger = count_playable_country
#			mode = subtract
#			mult = 10
#		}
#		round_to = 50 	#we want a solid number
#		min = 50		#and we don't want less than 50
#	}
#	recurring = yes
#}

crisobj_extradimensional_galcom_resolution = {
	reward = {
		base = 500
		complex_trigger_modifier = { #subtract a bunch for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 25
		}
		round_to = 100 	#we want a solid number
		min = 250 		#and we don't want less than 50
	}
	recurring = yes
}

crisobj_have_bio_or_horizon_brain = {
	reward = {
		base = 800
		complex_trigger_modifier = { #subtract 50 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 50
		}
		round_to = 200 	#we want a solid number
		min = 200		#and we don't want less than 200
	}
	recurring = yes
}

crisobj_use_reality_perforator  = { #Also include the Plasmic Core ?
	reward = {
		base = 1000
		complex_trigger_modifier = { #subtract 20 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 10
		}
		round_to = 100	#we want a solid number
		min = 200		#and we don't want less than 50
	}
}

crisobj_have_m_brain = {
	potential = {
		has_gigastructures = yes
	}
	reward = {
		base = 850
		complex_trigger_modifier = { #subtract 50 for each playable country that exists
			trigger = count_playable_country
			mode = subtract
			mult = 50
		}
		round_to = 200 	#we want a solid number
		min = 200		#and we don't want less than 200
	}
	recurring = yes
}


#### Behemoth Fury ####
crisobj_have_bio_brain = {
	reward = {
		base = 500
		complex_trigger_modifier = {
			trigger = count_playable_country
			mode = subtract
			mult = 25.0
		}
		round_to = 25
		min = 250
	}
	recurring = yes
}