### Zones for Special Planets

### HORIZON BRAIN CONFIGURATIONS
# Buildings do the job stuff
zone_hb_server = {
	icon = GFX_colony_type_synaptic_lathe_colony
	base_buildtime = @zone_buildtime
	#potential = {} # planet scope

	#unlock = { # planet scope
	#	NOT = {
	#		has_designation = col_resort
	#	}
	#}

	#resources = {
	#	category = planet_zones
	#	cost = {
	#		minerals = @zone_cost
	#	}
	#}

	potential = { # planet scope
		hidden_trigger = { exists = owner }
		owner = {
			is_wilderness_empire = no
		}
	}

	unlock = { # planet scope
		hidden_trigger = { exists = owner }
		owner = {
			is_wilderness_empire = no
		}
	}

	included_building_sets = {
		horizon_brain
	}

	#max_buildings = 4 #6 ### Needs testing

	zone_sets = {
		ssh_horizon_brain_world
	}

	resources = {
		category = planet_zones
		cost = {
			alloys = @zone_cost
		}
	}

	inline_script = {
		script = jobs/zone_ssh_horizon_brain_configuration_jobs_add
		SPECIAL_USER_AMOUNT = 400
		SPECIAL_USER_HALF_AMOUNT = 200
		#AMOUNT = $AMOUNT$
	}

	planet_modifier = {
		zone_building_slots_add = 3
	}

	#ai_resource_production = {
	#	alloys = 20

	#	trigger = {
	#		has_any_foundry_zone = yes ##### TODO: Add resource stuff for each type of configuration building
	#	}
	#}

	ai_priority = 10 #2
	ai_weight_coefficient = 1.2 #0.5
}